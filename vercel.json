{
  "installCommand": "pnpm install && npm --prefix frontend install",
  "buildCommand": "pnpm run typecheck && npm --prefix frontend run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
